"""
AI App Builder backend application.

This package contains the FastAPI application that coordinates:
- Planner agent: breaks user prompts into build steps
- Coder agent: generates files from build steps
- Sandbox: executes generated code
- Debugger agent: diagnoses failures and suggests fixes
"""
